{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: mycluster
  podMetricsEndpoints:
  - port: metrics
{{ end }}